#include <iostream>
#include <regex>

int main(int argc, char *argv[]) {
    int index = -1;
    index = std::max(1, 20);
    index = std::max(index, 10);
    std::cout << index<< std::endl;
    return 0;
}